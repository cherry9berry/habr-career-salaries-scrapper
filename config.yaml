# Database configuration
database:
  # Supabase PostgreSQL configuration
  host: db.cehitgienxwzplcxbfdk.supabase.co
  port: 5432  
  database: postgres
  user: postgres
  password: !!!!QQQQ2222
  
  # Local PostgreSQL (commented out)
  # host: localhost
  # port: 5432
  # database: scraping_db
  # user: scraper
  # password: your_password

# API configuration  
api:
  url: "https://career.habr.com/api/frontend_v1/salary_calculator/general_graph"
  delay_min: 1.5
  delay_max: 2.5
  retry_attempts: 3

# Scraping limits
max_references: 2000 